---
title: toolbar settings surface
state: horizon
created: 2026-08-17
tags: [feature]
---

where pane's settings gather when it grows them: the toolbar exposes every control the app has today (model, system prompt, tools, export), so the first real settings surface needs a home. the candidates are a settings control on the bar (a modal like the system prompt's, per the bar's overflow seam) or a settings page — decide when the first setting beyond model and prompt demands it.

## why

the metawoo toolbar arc (built 2026-08-17, journal 2026-08-17) named this the last item on its deferred menu: a product question, not a chrome decision, parked until there is a setting to set.