# 2026-08-25

- context-usage-tracking merged to main by michael (fast-forward; main now at `b0dedd8`, the branch's tip). the branch carried four work streams: the context usage tracking arc and the metawoo toolbar arc (both closed out before merge), the persistent sessions arc (stages 1 and 2 landed, closeout still open), and the standalone `maxTokens` commit on the tip.
- the local `context-usage-tracking` branch is deleted; `origin/context-usage-tracking` still exists on the remote and can be deleted at michael's discretion.
- persistent sessions arc on main, state of the closeout: `docs/current/pane.md` carries the built behavior and the accepted residuals (reload-scoped sync, one-slot error line, no clear-all, no auth on the session endpoints, unload/crash tail loss, failed-destructive-op detach); the changelog already holds the disk-sessions `FEATURE`, the rail-ordering and clear-all-removal `CHANGE` entries; the remaining steps are michael's manual browser pass (the work order's pass, items 1-22), then the closeout — the card flip is his call, the two `docs/future/` intent documents are removed by the agent, and `mercurius.yaml`'s `review_focus` returns to its no-arc-in-flight note with this arc's guards retired (the cross-arc thinking and usage guards carry forward). the card's frontmatter currently reads `state: evaluating` — the journal of record for the flip to building is 2026-08-18, so the state field and the journal disagree and the flip should land with the closeout.
- agent memory: this session (combined design + planning + implementation role, bootstrapped off the grimoire's AGENTS.md) summarized the pending merge before it landed and is the agent on record for the closeout when it comes.
- persistent-sessions closeout (implementation agent, michael's go-ahead after the manual browser pass passed): `docs/future/persistent-sessions{,-work-order}.md` removed — the realized value is in the code and `docs/current/pane.md` (the error-handling table carries the six accepted residuals, the deferred list's cross-tab item carries the reload-scoped / server-assigned-versioning entry). the spec's remaining deferred items (no import, retention/pruning, data-directory portability) are not re-landed as cards: the converged closeout contract carries the residuals to `docs/current/` and the versioning item to its card, and the rest stay in the journal of record — a card lands if one of them is ever the subject of a call. the `server-assigned-session-versioning` card's `## why` pointed at the spec's deferred menu; it now points at `docs/current/pane.md`'s deferred list, the one dangling reference the deletion sweep found (the `frontend-tests` and `server-assigned` cards' other arc references name the arc, not the document, and stay valid).
- epic card `persistent-not-localstorage-sessions`: removed entirely by michael (with its `order.yaml` line) rather than flipped to done — the card's value now lives in the journal of record and the built code. the four cards the arc spawned (`frontend-tests-for-the-session-hook`, `renameretitle-conversations`, `server-assigned-session-versioning`, `write-only-tool-approval`) stand as the live forward-looking remainder.
- mercurius state: reset by michael — `mercurius.yaml` is back to its no-arc-in-flight form and `.mercurius/` is gone; no arc is in flight on main.
- rename conversations (implementation agent, card `renameretitle-conversations`, census confirmed by michael: no re-stamp, empty title reverts to derivation, blur cancels, hover-only glyph): a pencil on a rail row swaps the title for an inline editor — enter commits, escape or blur cancels, the input opens focused with its text selected. `ConversationList` owns the transient `editingId` + draft; only a commit crosses into the working copy through `onRename`. the app handler is a `setConversations` map that changes only `doc.title`, so `messages` and `usage` keep their references (the read-is-not-a-write snapshot is unaffected) and `updatedAt` is never re-stamped — a rename is not activity, and the row keeps its place in the rail. empty title means no explicit title, so the derived title applies again, the existing `''` semantics of the commit effect and the export path. the handler early-returns during the destructive-pending window like new/send/select, so a rename's save cannot re-create a file an in-flight delete just removed. the glyph is a hand-drawn material symbols 'edit' on the family's 960 grid, hover-visible like the delete mark, and `:focus-within` keeps it visible while the input holds focus. docs: the `Conversation.title` comment, the rail-ordering note, and the conversation-management UI bullet in `docs/current/pane.md`; changelog entry added. card flip/close is the operator's act, as with the persistent-sessions card.
- drive-by, recorded for michael to reject: the `maxTokens` commit (2026-08-20, `b0dedd8`) landed the `max_tokens`/`default_max_tokens` knobs and the `empty_response` error code with `docs/current/` coverage but no changelog entry; one is now added to Unreleased.
- pencil glyph replaced (michael: the hand-drawn edit path read mangled — use the material pencil): the `EditIcon` path is the real material symbols `edit` glyph, extracted from the official `google/material-design-icons` variablefont TTF (`MaterialSymbolsOutlined`, codepoint `0xf097`), instantiated at wght 500 to match the family's other glyphs, and mapped onto the set's 960 box (x' = 0.96x, y' = -0.96y — the font's top landing on the box's -960 top, the same convention every sibling in `icons.tsx` uses). the first extraction was already correct under that mapping; my ASCII rasterizer had the box upside down (row 0 treated as y 0, the box's bottom, instead of y -960, its top), which made the siblings render flipped in my verification and sent me on a y-axis 'correction' that shipped the pencil inverted while leaving the correct siblings untouched. michael confirmed the siblings were right and the pencil inverted; the path is back on the correct mapping, re-verified by rasterizing siblings and pencil under the true mapping (upload arrow up over its tray, wrench head top-left, pencil head top-right and tip bottom-left), and the stage re-gated by terminus. the flipped-siblings finding recorded earlier today is withdrawn: it was the rasterizer, not the tree. terminus re-gate dfdcb0b015ab came back clean with one advisory (truthful-naming-001, non-blocking): my comment labeled the glyph 'grade-500' while the extraction instantiated the font's weight axis at 500 — material symbols keeps wght and grade separate. adopted per michael: my line now reads weight-500 with the axis named, and the siblings' pre-existing 'grade-500' labels stay untouched, a sweep for another day. stage re-gated after the fix. the re-gate's re-raise of the advisory surfaced a sharper framing — my weight-500 line made the file's uniform grade-500 convention inconsistent inside itself — and the full sweep landed per michael: the header and the four labeled siblings now read weight-500 too (add-box and description never carried the label), the file is uniformly truthful, and the advisory is closed for the set.