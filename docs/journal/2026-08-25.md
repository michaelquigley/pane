# 2026-08-25

- context-usage-tracking merged to main by michael (fast-forward; main now at `b0dedd8`, the branch's tip). the branch carried four work streams: the context usage tracking arc and the metawoo toolbar arc (both closed out before merge), the persistent sessions arc (stages 1 and 2 landed, closeout still open), and the standalone `maxTokens` commit on the tip.
- the local `context-usage-tracking` branch is deleted; `origin/context-usage-tracking` still exists on the remote and can be deleted at michael's discretion.
- persistent sessions arc on main, state of the closeout: `docs/current/pane.md` carries the built behavior and the accepted residuals (reload-scoped sync, one-slot error line, no clear-all, no auth on the session endpoints, unload/crash tail loss, failed-destructive-op detach); the changelog already holds the disk-sessions `FEATURE`, the rail-ordering and clear-all-removal `CHANGE` entries; the remaining steps are michael's manual browser pass (the work order's pass, items 1-22), then the closeout — the card flip is his call, the two `docs/future/` intent documents are removed by the agent, and `mercurius.yaml`'s `review_focus` returns to its no-arc-in-flight note with this arc's guards retired (the cross-arc thinking and usage guards carry forward). the card's frontmatter currently reads `state: evaluating` — the journal of record for the flip to building is 2026-08-18, so the state field and the journal disagree and the flip should land with the closeout.
- agent memory: this session (combined design + planning + implementation role, bootstrapped off the grimoire's AGENTS.md) summarized the pending merge before it landed and is the agent on record for the closeout when it comes.
- persistent-sessions closeout (implementation agent, michael's go-ahead after the manual browser pass passed): `docs/future/persistent-sessions{,-work-order}.md` removed — the realized value is in the code and `docs/current/pane.md` (the error-handling table carries the six accepted residuals, the deferred list's cross-tab item carries the reload-scoped / server-assigned-versioning entry). the spec's remaining deferred items (no import, retention/pruning, data-directory portability) are not re-landed as cards: the converged closeout contract carries the residuals to `docs/current/` and the versioning item to its card, and the rest stay in the journal of record — a card lands if one of them is ever the subject of a call. the `server-assigned-session-versioning` card's `## why` pointed at the spec's deferred menu; it now points at `docs/current/pane.md`'s deferred list, the one dangling reference the deletion sweep found (the `frontend-tests` and `server-assigned` cards' other arc references name the arc, not the document, and stay valid).
- epic card `persistent-not-localstorage-sessions`: removed entirely by michael (with its `order.yaml` line) rather than flipped to done — the card's value now lives in the journal of record and the built code. the four cards the arc spawned (`frontend-tests-for-the-session-hook`, `renameretitle-conversations`, `server-assigned-session-versioning`, `write-only-tool-approval`) stand as the live forward-looking remainder.
- mercurius state: reset by michael — `mercurius.yaml` is back to its no-arc-in-flight form and `.mercurius/` is gone; no arc is in flight on main.