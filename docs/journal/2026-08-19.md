# 2026-08-19

- mercurius round 7 (follow-on session s_AgitNnBHXmqa, opened after midnight; a fresh MCP connection after the registry lost the session again at start): needs_changes — the cleanest round of the arc: two majors, zero advisories, both on the frontend state layer; the backend store and api have now passed clean for six straight rounds. C1 (the read-is-not-a-write guard compares the snapshot against the hook's live state by reference, but the two sides normalize the document's optional `usage` field differently — the snapshot seeded raw from `doc.usage` holds `undefined` for a document that omits the key while `loadConversation` normalizes to `null`, so opening such a document reads as changed and issues a write, re-stamping `updatedAt` and reordering the rail on a pure read) settled with michael after he rejected the reviewer's premise: there are no pre-usage-arc documents — pane is pre-1.0, one operator, the usage arc landed the previous day, and the reviewer's "legacy documents in a long-lived profile / likely when usage reporting is disabled" framing was cold-context speculation, not a property of this deployment. the finding stands on its internal leg only: the conversation shape declares `usage` optional (`usage?: UsageRecord | null`), so a document that omits the key is valid, and the guard as written is wrong for a shape its own type declares. fixed as one line of normalization — the snapshot is seeded `doc.usage ?? null`, the same normalization `loadConversation` applies, so both sides of the identity comparison live in the same normalized space — stated in the spec's commit-triggers bullet, the work order's remote-id transition branch and commit-effect bullet, and a fixture clause in manual pass item 13 (a document omitting `usage` opens with no PUT and no rail movement). no mechanism added; the phantom-scenario justification is dropped from the artifacts. C2 (a failed `/api/config` leaves the app permanently inert without the visibility the round-5 settlement claimed — the round-5 rationale cited "every chat send failing", but the round-3 send gate refuses a send while mode is null *before any request goes out*, so that surface never lights, and `useConfig` swallows the fetch error into `emptyConfig`, leaving the empty model list and disabled send button with nothing that says why) fixed as michael approved after confirming the shape: show and wait — the operator, not the app, is the recovery path. `useConfig` now exposes the fetch failure as a state field instead of catching it into `emptyConfig`, and the app renders it through the arc's existing quiet error line once loading has ended; no retry (the round-5 decision stands — the client would be hammering a fetch its own binary just failed to serve), no new chrome. the round-5 text's substance (mode stays null, recovery is restart-and-reload) is preserved; only its broken visibility justification is replaced. work order: the failed-config bullet rewritten, the `useConfig` critical-files row, the error-line bullet (a config-failure line persists until reload), the error-coherence bullet, and a config-edges manual check (a devtools rule blocking `/api/config` in either mode); spec: the `useSessions` mode clause now states the failed-fetch case, the app-renders-the-error bullet carries it, and what-does-not-change names it among the visible additions. round 7 is fully triaged — two majors, zero advisories — and the artifacts are ready for round 8.
- mercurius round 8 (same session s_AgitNnBHXmqa, round 2): ready_to_build — zero findings, zero questions, zero advisories. the reviewer: "i would build this under the stated constraints... the previously subtle normalization and config-failure visibility gaps are resolved, and no remaining issue materially blocks a single supervised implementer." the design arc has converged: across the eight review rounds the audit moved from store shape (1) to state transitions (2-3) to timing (4) to the deployment boundary (5) to contract correctness (6) to the last normalization/visibility gaps (7) — and found nothing further in round 8. closeout notes recorded; the two design artifacts are implementation-ready and the next pipeline step is stage 1 of the work order (backend store + api). the roadmap card flip and any commits remain the operator's acts.
- top-to-bottom coherence read of both artifacts (michael's pre-build request): the docs hang together — every spec↔work-order seam checks out (config/store/api tables, the FIFO+barrier+coalescing triad, the transition protocol, the round-7 normalization and config-failure wiring in both artifacts, residuals↔closeout, all manual-pass cross-references resolve). one real defect found and fixed: disk-mode manual pass item 7 ("`/api/sessions` never appears in the network panel") stated the *local*-mode invariant inside the disk-mode list — a healthy disk-mode build issues the list call on load, so the item would fail its own test; reworded the slot into the true and useful no-polling invariant (no `/api/sessions` traffic at rest after the initial list settles; calls only on selection, commit, or delete), keeping the item number since items 8-23 are cross-referenced from the stage-2 verification bullets. wording scars swept: a stray "—;" in the spec's diskSessionStore bullet; the error line's four drifted names ("storage-error line" / "quiet line" / …) unified to "error line" across both docs — "storage-" was a misnomer after round 7 made the line carry config failures too; the Save bullet's pre-round-4 fossil "mutex around the read-then-write of nothing" replaced with the true statement (rename keeps names whole for List, the mutex keeps a concurrent Clear from sweeping mid-Save); closeout and the docs bullet now name all six accepted residuals for carriage into docs/current (the two frontend ones — unload/crash tail loss, failed-destructive-op detach — previously evaporated at closeout); item 13's fixture sentence smoothed. all four docs pass unfurl; the artifacts remain implementation-ready.
- disk-only ruling (michael, pre-build): "let's proceed with disk-only" — the localStorage/local mode is eliminated from the design. grounding in code first: /api/config keeps its other duties (models, system prompt, context windows all flow from it, and selectedModel derives from it), so config-fetch failure still breaks *chat* but no longer gates *anything else* — the store never waits on it, the mode-readiness race machinery (rounds 3 and 7) dissolves entirely, and the C2 error line survives on a simpler, truer justification: its consequences (empty model list, sends failing without an explicit model override) would otherwise be unexplained. the composer gate loses its mode clause — canSend is the identity invariant alone, and a send pressed before config lands behaves exactly as today's app does (attempt it, fail visibly through the existing chat-error path). rework applied as a bounded diff to both artifacts: removed the `sessions` config key (and its validation, the `sessions: blob` startup edge, and the /api/config `sessions` field), the 503 `sessions_disabled` path, `localSessionStore`, the mode gate and null-mode branches through `useSessions`, the mode-switch manual items (19/20 retired; 21-23 renumbered 19-21, all verification cross-references re-pointed), the local-mode manual section, and the local-mode quota residual (five residuals remain). kept verbatim: the entire state machine (FIFO, tail coalescing + barrier, transition tokens, seededIds, clear-all sequence, usage normalization, error line). reframed rather than deleted: no-migration became no-import (pre-arc localStorage data is never imported; the retained active-selection id is cleared by the identity guard on first load — the upgrade case, which manual item 9 now checks); clear-all's `localStorage.clear()` now sweeps view state plus pre-arc legacy keys; seam census item 4 replaced (localStorage-first-class → disk-only); scenario s2 (stateless container) demoted to a shape-section note — the container story degrades (default data dir dies with the container; point data_dir at a mounted volume) instead of contrasting with a local default. /api/config is now entirely unchanged by the arc. mercurius.yaml recalibrated on disk: `local-mode-first-class` guard replaced by `disk-only-storage`, fail-fast/browser-view-state/rail-ordering guards reworded off the two-mode premise, review_focus rewritten for disk-only. the `server-assigned-session-versioning` card's "storage-error line" scar fixed to the unified name. the roadmap card (already flipped to building by michael) needed no change — its title is literally "persistent (not localStorage) sessions". artifacts rewritten, unfurl-green; one more mercurius round follows to close the rework.
- mercurius round 9 (new session s_3YmSkWBJCyrT, round 1 — first round over the disk-only artifacts): needs_changes — two majors, two advisories, all in the frontend contract layer; the backend store and api, untouched by the rework, are clean again. C1 (the `Promise<void>` save contract plus two sections that disagree on who advances `savedSnapshot` after a coalesced write: the hook section says the snapshot advances from the document the write persisted, the App commit section says each caller advances to its own references — a superseded caller's continuation fires after the newer document was written and can re-stamp the snapshot with the discarded document) fixed by michael's option 1: the hook's `save` resolves a verdict — `true` when the caller's own document was the one the shared write persisted, `false` when a newer same-id save superseded it — and only a `true` resolution advances `savedSnapshot` (to its own references, which are the persisted document's references); a superseded caller settles without touching the snapshot. five sites (spec hook bullet + commit bullet; work-order hook bullet, savedSnapshot state bullet, commit bullet). C2 (the error line's "next successful operation clears it" rule let an unrelated success — a `get` for another conversation — erase a failed save's warning while the transition discarded the unsaved tail the reader had seen, and returning later loads the older disk version with no trace of the loss) fixed as proposed: a failed save is now sticky the way a failed list is, cleared only by a success that settles it — that conversation saved successfully again, deleted, cleared, or a reload; unrelated successes (other gets, other ids' saves) no longer clear it. five sites plus a navigate-away-and-back clause in manual item 12. A1 (the 0755 dir / 0644 document modes expose conversation history to other local users where the home directory is traversable) taken: the store's contract now reads `0700` directory / `0600` documents, and the fix is a simplification — `os.CreateTemp` already creates temps at `0600`, so the `Chmod 0o644` step is deleted and a document is owner-readable from its first byte, the `0700` directory being the real gate. A2 (the hook's coalescing/barrier/token invariants are guarded only by the manual pass; the ui has no test runner) rejected by michael — the manual pass is sufficient for a supervised pre-1.0 build — and parked as the new roadmap card `frontend-tests-for-the-session-hook` (inbox, log 2026-08-19), referenced from the work order's closeout and guarded against re-raise in mercurius.yaml (`frontend-tests-parked`). all four findings triaged; round 2 follows to close the rework.
- mercurius round 10 (session s_3YmSkWBJCyrT, round 2 — verification pass over the disk-only artifacts with the round-9 fixes in place): needs_changes — one major, zero advisories, zero questions. the backend store and api are implementation-ready and the frontend ordering/snapshot/transition contracts are otherwise unusually precise. PS-R2-C1 (the destructive protocols — delete of the active conversation, clear-all — fence two of the three save paths, aborting the stream and detaching the commit owner, but leave the UI interactive while the destructive op awaits the FIFO; the two handlers that call the store's save directly — new conversation, and send's no-active branch — bypass both fences, so a save they enqueue in the pending window runs after the wipe and undoes it, most visibly clear-all coming up not-empty after an explicit delete-everything). michael took the fix over parking it: it completes a fence the protocol already built rather than adding a new defense, it is the app's own interactivity during a window the async op deliberately creates (not an external rare event like the beforeunload tail he declined), and the cost is small. fixed as a synchronous `destructivePending` ref, raised before the destructive op is enqueued, folded into `canSend`, and early-returning the new-conversation / send / selection(transition) / further-delete-clear handlers until the op settles; composer text stays editable; on failure only the guard is released and the round-6 detached-owner residual stands untouched. applied at seven work-order sites (state, transition, new, send, canSend, delete-active, clear-all) plus a dedicated verification bullet and a new manual-pass item 22 (work initiated while a destructive op is pending is refused — no document created, no selection change, the destructive op still lands), and the spec's composer + delete/clear bullets. round 3 follows as the closing verification pass.
- mercurius round 11 (session s_3YmSkWBJCyrT, round 3): needs_changes — two majors, zero advisories. (a) r3-error-lifecycle: the round-9 sticky-clear rule made a failed save persist until settled, but the hook still exposed a single overwritten `error` value, which cannot hold two simultaneously unsettled failures (failed saves of A and B; a failed save plus a stale-rail list failure) — a success on the other conversation cleared the field and A's warning about state that still differs from disk vanished. fixed as the operator took it: the hook tracks outstanding failures by scope — one `collection` scope (failed list/clear) plus one per conversation id (failed save/get/remove) — and exposes one derived `error`: the most recently recorded outstanding failure, dropping to the next-most-recent when the most recent settles. settlement rules are now defined per scope and per op kind: a success settles only the scope it settles (a successful get settles only a failed get, never a failed save — a read cannot settle a write's divergence); a successful list or clear settles the collection; a successful clear settles all scopes; a failed save persists until that conversation is saved again, deleted, cleared, or a reload. five sites across both artifacts plus the error-coherence verification bullet. (b) r3-read-envelope-validation: the store checked the document envelope (body id == path id) on write but not on read — List projected the body's id without comparing it to the filename, and Get only checked "is a JSON object" — so a hand-renamed or hand-edited file passed through as a phantom rail entry (opens to nothing) or a GET that violates activeDoc.id === activeId. rather than the reviewer's read-side twin of the write check, the operator took the root fix after probing it in two questions: why does the document need an id field when the filename already carries it (it doesn't — the id is the store's key, and the body id was a second copy whose mismatch class *was* the finding), and why not use the filename everywhere the body id is used (why not). settled shape: the filename is the sole id; `Conversation` drops `id`; the store interface is `save(id, doc)` / `get(id) → doc | null` and the body is byte-transparent in both directions (no stripping on save, no injection on get — the id the caller asked for is the document's id, which is addressing, not translation); `Save` validation shrinks to strict JSON-object intake (the `+extra`-only bind shape, the id-match check deleted); `List` takes the id from the filename stem; the identity invariant rewords from `activeDoc.id === activeId` to "activeDoc is null, or the document the store holds for activeId" — established by the transition protocol, guarded by `activeDoc !== null` plus the existing owner ref; `loadConversation` was verified to read only messages/usage, so `useChat` stays untouched. the finding is retired as moot, not fixed: with no body id there is no mismatch class — a renamed file is a conversation under its new name (stage-1 curl check + manual item 2 exercise this), a hand-edited body is the same conversation with new content, and the only unloadable state is a body that is no longer a JSON object, which gets the directed failure the spec's corruption residual now promises for the whole class. applied at 13 spec sites and 26 work-order sites; mercurius.yaml recalibrated (opaque-session-document, projection-casing-parked guards, review_focus) so the next round reviews the settled shape. both artifacts unfurl-green; round 4 follows as the verification pass.
- mercurius round 12 (session s_3YmSkWBJCyrT, round 4 — verification pass over the filename-as-sole-id rework): needs_changes — two majors, one advisory, all of them seams the round-3 fixes left slightly open; the rework's machinery itself (store validation, transition protocol, FIFO/coalescing) came back clean. (a) r4-error-scope-collision: round 3's per-scope error ledger held two rules that contradict — a new failure overwrites its scope's message, and a successful get must never settle a failed save — so a save-of-A fails → a get-of-A fails (overwrites A's slot) → a get-of-A succeeds (clears it) silently erases A's unsaved-tail divergence, reopening the exact silent-loss hole rounds 9 and 11 closed. fixed as the reviewer proposed, taken: per-class entries — a sticky write entry per conversation id (a failed save/remove; settled only by that id's successful save/remove, a successful clear, or a reload), a transient read entry (a failed get; settled by the next successful get), plus the collection entry; a new failure overwrites only its own class's message, so a failed get can never erase an open save divergence, and the displayed line stays one derived error across all outstanding entries, dropping to the next-most-recent as each settles. applied at the spec's hook + error-render bullets and the work order's hook bullet, error-line bullet, error-coherence verification, and manual item 12 (extended with the same-id reselection case proving a read success settles only the read entry). (b) r4-session-id-addressability: the round-3 rename promise (a renamed file lists, opens, saves, and deletes under its new name) had two holes in the filename→URL pipeline — `List` published every `*.json` stem without the `safeID` rule the item endpoints enforce (a file literally named `.json` lists as an id no `GET` can address: a ghost entry), and the adapter concatenated ids into item URLs unencoded, so a disk-safe name like `issue#1.json` truncates the request at the fragment separator and 404s. fixed with both of the reviewer's clauses, taken in full: `List` skips (with a warning) stems failing the same `safeID` rule, so the rail publishes exactly the set the item endpoints can address; the adapter builds every item URL as `/api/sessions/` + `encodeURIComponent(id)` — the mux hands the handler the decoded segment, so `issue%231` arrives as `issue#1` and passes `safeID` unchanged, and the backend gains no code. directed checks added: the store-test row covers the URL-reserved name and the List-skip case, stage 1 curls a round trip through `/api/sessions/issue%231`, and manual item 2 names the URL-reserved rename case. (c) r4-delete-failure-state (advisory): spec and work order disagreed on the post-failure screen of a failed active delete — the spec's residual keeps the conversation rendering; the work order's handler nulled `activeDoc`/`activeId` before the delete settled and never restored it on failure. fixed by taking the spec's side: the handler clears the selection only on success; on failure the selection is untouched and keeps rendering the conversation the store still holds, under the error line — the same principle as a failed save keeping the unsaved tail visible. applied at the work order's handler bullet, `destructivePending` bullet, destructive-pending verification, and a new manual item 23; the spec's residual already said the right thing and is unchanged. all three findings taken; both artifacts unfurl-green; round 5 follows as the verification pass.
- mercurius round 13 (session s_3YmSkWBJCyrT, round 5 — verification over the round-4 fixes; the round-4 fixes themselves came back clean): needs_changes — two majors, one advisory, deeper seams in the same contracts. (a) R5-C1: the save-queue bullet promised two contradictory things — an absolute every-moment bound of at most one in-flight and one pending save per id, and a barrier rule requiring a fresh same-id entry behind any intervening operation; save(A1) pending, get(B) enqueued, save(A2) must be a second pending entry for A to preserve invocation order, which the bound forbids, and re-merging would move A2 ahead of get(B). fixed as the reviewer proposed, taken: the bound now holds only within an uninterrupted tail run — coalescing applies while the save's entry sits at the queue's tail, and two pending entries for the same id are legal (and never re-merged) whenever a barrier separates them; the backlog sentence, the verification bullet, and manual item 21 (now conditioned on nothing interleaved) all carry the tail-run wording, and the operator-observable property (no pile of PUTs in the no-interleaved case) is unchanged. (b) R5-C2: `destructivePending` was specified as a ref while driving the `canSend` prop — a ref does not re-render, so a ref-only implementation leaves `ChatView` with a stale enabled button whose Enter path clears the typed input before the app-level guard refuses the send, silently breaking the settled refused-sends-keep-the-text requirement; both interpretations compiled. fixed as the reviewer proposed, taken: `destructivePending` is reactive boolean state (what closes the component-level gate and re-renders the composer) plus a mirror ref that the handlers' same-turn early-returns read, updated by one helper — the state bullet rewritten with the render requirement named, the verification bullet pins the re-render check, and item 22 gains the observables (button visibly disabled, typed text retained on a refused Enter). (c) R5-A1 (advisory): the single error line had no stated precedence when a persistent config-fetch failure and a session failure are both outstanding (the load-against-dead-backend case) — an implementation could let one mask the other permanently. fixed as the reviewer proposed, taken: the ordering is `sessions.error` when non-null, otherwise the config error — a session failure takes the line over the persistent config failure, and the config failure resurfaces when the last session entry settles; a persistent failure can never mask a live one. applied at the spec's error-line bullet, the work order's error-line bullet, and manual item 17 (now stating both sources fail at load and the reload settles both together). all three findings taken; both artifacts unfurl-green; round 6 follows as the verification pass.
- mercurius round 14 (new session s_i7ch8uWjlRVn, round 1 — the old session s_3YmSkWBJCyrT became unrecoverable after the mercurius server process restarted mid-review; its five rounds and the round-5 notes, which I wrote to disk by hand in the on-disk format, remain under .mercurius/): needs_changes — one major, one advisory. C-01 (the destructive-pending guard closed the composer but not the existing Retry action — verified in code: ChatView's standalone retry button wires useChat's retryLastRequest, and a retry pressed during a pending delete/clear starts a turn whose commits stream without being mirrored after the owner detach; item 22 did not exercise the path) — the operator took it in reduced form (retry gated by the same reactive guard), but before I applied it he asked the bigger question that made it moot: the localStorage model never had any of this infrastructure — why not just surround the on-disk facility with an interface giving it the same semantics as localStorage? I verified against the actual code first: useLocalStorage is read-once-on-mount, mirror-on-every-write, state-is-the-truth-for-the-tab's-life, and App.tsx runs entirely on that shape (selection = synchronous find; every mutation a setConversations whose only side effect is the mirror) — so the app already had the model, and every piece of machinery the disk design added (the FIFO queue, save coalescing + tail-run barriers, the per-class failure ledger, transition tokens, the activeDoc protocol) existed only because the design made the *store* the source of truth. the operator settled the trade: live cross-tab sync out, reload-scoped sync in — "that's fine for the next version of pane, especially if it dramatically simplifies the model." A-01 (the rail's tiebreak direction was unspecified — backend and frontend could order a same-updatedAt pair oppositely after reload) fixed as the reviewer proposed: id ascending in both the store's List and the rail's display sort, now seam-census item 10. both artifacts reworked around the mirror model: React state is the working truth, the disk store its durable mirror (hydrated on load, persisted on change), selection is a synchronous find, the hook is shaped like useLocalStorage — a diff-mirror of saves over setConversations, explicit remove/clear calls the destructive handlers await, a handful-of-lines serial mutation chain so a clear issued after a save cannot overtake it, and the one-slot error line that is the visible form of the app's historical console.warn for localStorage writes. the read-is-not-a-write guard survives in reduced form (savedSnapshot seeded from the loaded conversation's own references — loadConversation, verified, seeds messages from the conversation's own array reference and the usage record from its record, so the identity comparison holds), as do the destructive-pending guard (state + mirror ref, the R5-C2 form), the composer/retry gates, the meter's derived input, and all of stage 1's store/API machinery unchanged. state-first destructive semantics now define the failure screen: a failed remove leaves the screen in the post-delete state and a reload restores from the disk, which still holds the conversation; a failed clear wipes nothing (store.clear precedes localStorage.clear + reload). the work order's frontend half shrank from ~54 KB to ~38 KB; mercurius.yaml gained the mirror-model-runtime guard and a recalibrated review_focus; the seam census gained the mirror-model and tiebreak entries. both artifacts unfurl-green; round 2 of the new session reviews the reworked design.
- mercurius round 15 (session s_i7ch8uWjlRVn, round 2 — first pass over the mirror-model rework): needs_changes — two majors, one advisory. the reviewer conceded the backend store and API are otherwise well specified and called both holes narrow fixes needing no new synchronization architecture. (a) ps-r2-c1-hydration-mutation-race: the loading gate closed send and retry but left the two new-conversation controls ungated, and handleNewConversation had no loading check — with a slow initial list, a new-conversation click could enqueue its PUT and move activeId, then the settling hydration snapshot would replace the working copy without the new conversation: a file on disk the rail never shows, an activeId the rail doesn't list, and no error naming the divergence; the same race applied to clear-all clicked during loading. the "the only reader is the initial hydration, which precedes any mutation" assumption was load-bearing for the absence of read-ordering machinery, and it needed a total gate to be true. fixed as the reviewer proposed, taken: the composer gate became the session gate — every session-mutating entry point (both new-conversation controls, send + enter, retry, clear-all) is closed while loading or destructivePending: controls render disabled, handlers early-return, and the hydration half needs no mirror ref because loading moves only true→false, so a state read is never stale in the danger direction (unlike the destructive guard, which moves both ways). (b) ps-r2-c2-clear-fence-reentry — my own error from the rework: I had written "selection changes stay permitted: a selection cannot undo an in-flight delete or clear", which is true for delete but false for clear, because the sync effect re-attaches commit ownership to whatever is selected; a selection during the pending clear then leaves ownership live, and any message mutation on the newly selected conversation — even a thinking-collapse toggle — commits a save behind the clear in the serial chain, which repopulates the file after the sweep with no error, breaking the central destructive-ordering guarantee. fixed as the reviewer proposed, taken: selection is refused during the destructive-pending window (the selection handler early-returns on the mirror ref, the entry-point list gains selection, and the wrong justification is replaced in both artifacts with the re-attach rationale), with the cost stated explicitly: a rail click dropped in the brief loopback-bounded window, re-click after settle works. (c) ps-r2-a1-id-collation (advisory): "id ascending" pinned the order but not the collation — Go compares codepoints, a frontend reaching for localeCompare would sort a same-updatedAt pair differently, and a reload could visibly swap two rows, defeating the "screen order and disk order agree" promise. fixed as the reviewer proposed, taken: the tiebreak is now id ordinal-ascending at all eleven sites (a global rename), with the collation pinned at the three canonical sites — codepoint order via the relational string operator in both languages (Go's string < and the frontend's <), never locale-sensitive comparison. both artifacts unfurl-green; round 3 follows as the verification pass.
- mercurius round 16 (session s_i7ch8uWjlRVn, round 3 — verification over the round-2 fixes, which came back clean; the reviewer now calls the backend store and API implementation-ready): needs_changes — one major, one advisory. (a) ps-r3-c1-hydration-selection-transition: the mirror rework never defined what happens to a *retained* selection when hydration finishes — verified against the real code: the sync effect is keyed only on `activeId` and `conversations` starts empty, so a retained `activeId` makes the effect run against the empty copy (no snapshot seed, owner stamped), and when hydration later fills the copy, `activeId` has not changed and the effect never re-runs — the rail lists the conversation with full history while the chat area shows the empty state, and a subsequent send builds its `/api/chat` request from empty history and overwrites the stored history on commit. two corollaries: a retained ghost id (pre-arc `pane:activeConversation` naming no stored session) was cleared of *chat state* but never of `activeId`, so its sends take the existing-conversation branch and stream work that cannot be mirrored to any conversation; and the hook said `loading` ends "when the first list settles" while the session gate needed it closed until all listed documents are installed — two incompatible readings of one flag. fixed as the reviewer proposed, taken: one **hydration-completion transition** — `loading` stays true until the working copy is fully installed (list settled + every listed id's get settled; a failed get drops its entry but still settles) and flips in the same step the copy is installed, one boundary one meaning; the sync effect is keyed on `activeId` and `loading`, is a no-op while loading, and at the flip reconciles the retained selection against the installed copy — a match loads its document and seeds `savedSnapshot` from the document's own references (so a send after completion builds from real history, never from the window's empty state), a ghost sets `activeId` to null so the effect's null branch re-runs and resets chat state, commit owner, and meter reading together (a ghost id can never be left as the commit owner, and a send after the clear takes the create branch). applied at the spec's selection bullet (rewritten around the transition), session-gate bullet (the single `loading` boundary), no-import paragraph, and the work order's hook hydration bullet, sync-effect bullet, snapshot bullet, a new stage-2 verification bullet, and manual items 1 and 8 (retained selection restored after hydration settles; ghost cleared at the transition). (b) ps-r3-a1-unicode-collation (advisory): round 2's "relational string operator in both languages" is wrong as a cross-language claim — Go compares UTF-8 bytes, JavaScript's string `<` compares UTF-16 code units, so a same-updatedAt pair containing supplementary-plane characters (an operator-renamed file carrying emoji, which the arc's rename promise supports) could order differently across the two sides. taken rather than parked: the tiebreak is UTF-8 byte order on both sides — Go's string compare is byte order natively, the frontend compares the id's `TextEncoder` bytes (a few-line helper in the rail's sort), never the raw-string operator and never a locale-sensitive comparison — applied at the spec's projection bullet, census item 10, and the work order's ConversationList bullet. both artifacts unfurl-green; round 4 follows as the verification pass.
- operator decision during round-4 triage (2026-08-19): **clear-all is removed from the design.** "i don't think we even need to support 'clear all' anymore. the on-disk directory can just be removed by the user." applied across both artifacts: the `DELETE /api/sessions` row and `handleClearSessions` leave the API (four `/api/sessions` rows remain), `Clear()` leaves the store interface, `clear` leaves the adapter, the hook's exposed set, and `handleClearAllData` (confirm, `localStorage.clear()`, reload) leaves App; the `destructivePending` guard narrows to delete-of-the-active-conversation, and the serial chain and its call-time enqueue (round 4's c3) stay — a `remove` issued after a save must not execute ahead of it and re-create the file. manual item 9 recasts as a mid-turn delete of the active conversation; item 15 (failed clear wipes nothing) is deleted and items 16/17 renumber to 15/16 with cross-references updated; item 5 now names the operator's file operation (stop pane, delete the data directory, restart and reload) and the pre-arc keys' fate. the two things clear-all uniquely did are covered elsewhere: the ghost `pane:activeConversation` is cleared by the round-3 hydration-completion transition, and the pre-arc `pane:conversations` copy — which no product action now touches, matching the no-import section's existing "until the user clears browser data" — leaves the profile only with a browser-data clear, recorded as a residual in both artifacts (the spec gains a "there is no clear-all" residual bullet; the work order's pre-arc residual and docs-section bullet carry it). the round-4 advisory ps-r4-a1 (partial-sweep semantics of a failed clear) is **superseded by this removal** — no clear, nothing to qualify — and is recorded as such in the round notes.
- mercurius round 16 (session s_i7ch8uWjlRVn, round 5): **ready_to_build** — zero concerns, zero questions. the reviewer: the artifacts are ready to build under the stated single-user, supervised, pre-1.0 constraints; the store mechanics, API behavior, mirror lifecycle, hydration gate, read-is-not-a-write guard, mutation ordering, destructive-operation fence, and verification pass are sufficiently specified without further decisions. two non-blocking advisories, both adopted as one-line work-order additions: ps-r5-a1 — the app mounts under React StrictMode, which repeats effect setup in development, so the hydration effect keeps an effect-local cancellation flag set by its cleanup and skips installing the copy and flipping `loading` when superseded (a late first-instance result can no longer overwrite a local change; a no-op in production); ps-r5-a2 — a negative casing test pins the projection's explicit camelCase dd tags (camelCase populates the projection, `dd`'s default snake_case does not). the session is ready for the operator's call: close it and move the arc to implementation per the work order's closeout section.