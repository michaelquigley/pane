# 2026-08-18

- persistent sessions arc, combined design + planning agent (this session): the `persistent-not-localstorage-sessions` card (flipped to building by michael) was shaped into `docs/future/persistent-sessions.md` (spec) and `docs/future/persistent-sessions-work-order.md` (two stages: backend store + api; frontend rewiring + docs). bootstrapped from the grimoire (AGENTS.md, agent-roles, design-build-pipeline, orienteering). the seam census records seven calls: six confirmed by michael - backend storage ownership with /api/chat protocol unchanged; one opaque JSON file per conversation in the frontend's camelCase shape; no migration (old localStorage data stays in the browser, exportable as markdown); localStorage retained as a first-class `sessions: local` compiled default with disk an opt-in config setting (michael's addition to the census - the mode is a deployment property, config-only, no UI toggle); active-conversation selection and chat preferences staying per-browser in both modes; `data_dir` default `~/.local/share/pane`, configurable. the seventh - the list projection speaking the document's camelCase rather than the api's snake_case - is my recommendation, recorded for michael to reject and left unguarded so the review may re-raise it.
- one visible behavior change landed deliberately in both modes: the conversation rail orders by last activity (updated descending) instead of insertion order - standard chat-client behavior, and the ordering a persistent record wants. named in the spec's shape section and guarded so it is not re-litigated as a local-mode regression.
- store design: `internal/session` package, per-store mutex, temp-file + rename atomic writes, `safeID` path containment, projection-only reads (id/title/createdAt/updatedAt), corrupt files skipped in the list with a warning, fail-fast startup in disk mode (no silent fallback to local). the frontend adapter interface (list/get/save/remove/clear) puts one code path over `localSessionStore` (today's `pane:conversations` key) and `diskSessionStore` (fetch); the app gates on the new `/api/config` `sessions` field and is inert until it lands. `useChat` is untouched - `loadConversation` already seeds the usage record from the conversation object, so the meter's reading round-trips through the store as the usage arc's revisit condition required.
- mercurius calibration re-pointed at the arc: the five metawoo-toolbar guards were retired (that arc closed 2026-08-17; its behavior lives in docs/current/pane.md, and review_focus now states that roadmap cards are deliberately parked, not gaps), the cross-arc guards carried (thinking trio, usage trio, observability-out-of-scope), and six new guards earned from this session's settled calls (local-mode-first-class, no-migration, opaque-session-document, browser-view-state-local, fail-fast-disk, rail-orders-by-activity). review_focus names the arc and states the appliance baseline inline for a fresh-context reviewer.
- handoff: spec + work order drafted and calibration set; mercurius review loop running (session s_D4PFZHu8r0v4).
- mercurius round 1: needs_changes — five majors, all on the frontend work order (the backend store shape passed clean). triaged with michael one at a time, all fixed in place: c1 the active-document identity invariant (the sync effect becomes the guarded fetch sequence; the mode-switch ghost-id case; manual pass 8-9); c2 mutation ordering (one FIFO promise chain per hook instance; clear-all drains it before reload; the residual scoped to cross-client); c3 silent persistence failure (the adapter checks the documented statuses, 404-on-get keeps its null meaning; the rail moves only on success; one quiet error line, the arc's single visible addition, named in what-does-not-change); c4 the literal tilde in the data_dir example — michael chose leading-tilde expansion in SessionDataDir() over an absolute placeholder, so the documented example works as written; c5 clear-all leaving the dormant pane:conversations key — now removed explicitly in both modes so a flip back to local cannot resurrect deleted conversations. plus a drive-by: a storeDir typo in the stage-1 sketch.
- mercurius round 2: needs_changes — five majors and one advisory, all on the frontend state-transition layer: round 1's words named the states, not the transitions. PS-R2-C1 the read-induced write (opening a conversation triggers the commit effect; the re-stamped updatedAt reorders the new last-activity rail from a mere selection, and the induced PUT can clobber another tab's change in the get-to-put gap — a reader clobbering a writer, beyond the last-write-wins residual) fixed as proposed with michael: savedSnapshot, the (messages, usage) references the store is known to hold — seeded from the fetched document's own references before loadConversation, advanced on each successful save; the commit effect skips on reference identity, so a read is not a write. michael asked whether a server-assigned version counter should prevent cross-client collisions: a client timestamp is not monotonic across tabs, and the mechanism is an addition to, not a replacement for, the snapshot guard — parked as the new roadmap card server-assigned-session-versioning (state inbox) and a named entry in the spec's deferred menu, with the revisit condition (two tabs or machines editing one conversation in real use) and a new mercurius guard (lww-residual-version-parked). PS-R2-C2 (the transition protocol) fixed with michael: a selection is a transition, not a state change — the sync effect becomes the named protocol (abort, null document and snapshot, clear the chat view to the empty state or seed it from the in-memory document, seed or fetch, ready only on a matching document); locally created conversations bypass the fetch via a seededIds set, retiring the current skipNextConversationSyncRef escape hatch; the composer keeps the input editable while the send button and enter path gate on a resolved selection (a canSend prop in ChatView, gate before the input clear so a refused send never discards typed text) — the micro-call (disable send, allow typing) was settled by michael and recorded as census item 8 plus the composer-send-gate guard. PS-R2-C3 (clear-all mid-turn repopulation) fixed as proposed: handleClearAllData cuts off the enqueuer before the clear — chat.abort() kills the in-flight turn and the commit owner is detached so the commit effect is a no-op — then awaits the store clear, so a live stream cannot enqueue a save after the clear and manual pass item 10 holds by construction. PS-R2-C4 (stale reads overtaking writes) and advisory A1 (the id tiebreak) fixed as proposed: `list` and `get` join the same FIFO chain as the mutations, so a read can never resolve with state older than the operations the tab invoked before it — a late list cannot repaint the rail backwards and a `get` cannot seed the snapshot from a stale document — with each entry recoverable so one failed operation never jams the queue; A1 folds in as local mode's `list` applying the same id tiebreak the disk store uses, making rail order fully deterministic in both modes. PS-R2-C5 (error-shape coherence) fixed as proposed: one small decoding helper in the disk adapter reads both documented error shapes — a plain `error` string (the shape the handlers actually emit) or the nested `error.message` object (the local-mode 503 shape) — falling back to the HTTP status text only when the body carries neither, so an over-cap save surfaces the backend's reason instead of a bare status text; the hook's error state covers every operation, not just mutations, so a rejected initial list or get leaves the rail or a selection empty *with* the error line rather than silently. all round-2 findings and the advisory are now triaged; round 3 ran on s_lt2MOh4s7E1Z against the fully re-applied artifacts (its first follow-on session, s_VvJfnaPOLFD3, was lost to a server restart before the round could start under it).
- artifact regression, repaired: between the C3 and C4 turns the two artifacts on disk had lost C2's changes in both files (the transition protocol, seededIds, the canSend composer gate, census item 8, manual-pass items 14-15) while C1 and C3 survived — the working tree had no external edit I made, so the cause is open; possibly an editor with a stale buffer. the C2 blocks were re-applied verbatim from the recorded decisions, C4's work-order blocks applied with them, and all markers verified present (seededIds, canSend, ChatView row, items 14-16, the spec's selection-transition paragraph and census item 8). michael handles the commits — the re-applied state is in the working tree for his review.
- michael's triage pass reshaped the roadmap tree around the arc: authentication (researching, epic) replaced authentication-and-accounts and explicitly scopes auth to persistent sessions with a persistent installation — the spec's residual and deferred pointers were re-aimed at it; census item 7's projection-casing question, recorded in the design session for the operator to reject, became its own card instead (camelcase-snakecase, researching) — the spec now records the parking decision and a guard (projection-casing-parked) keeps the reviewer off the casing question in this arc; metawoo branding absorbed pane-as-metawoo-product.
- mercurius round 3 (session s_lt2MOh4s7E1Z): needs_changes — four majors and one advisory, all on the frontend transition protocol again: the review is now auditing the protocol the C2 fix itself wrote. c1 (the seededIds bypass was permanent: a created id skipped the fetch forever, so navigating away and back stranded the selection with send disabled) fixed as proposed: the bypass is one-shot and conditional — skip the fetch only when the id is marked and activeDoc?.id === activeId, marker consumed during the initial transition — so a later visit to a created conversation fetches from the store like any other; the spec's selection-transition paragraph states the one-shot rule and manual pass item 18 checks the revisit case. c2 (mode readiness: the protocol and the send gate were acting while /api/config was still in flight — a fast first send could start a chat that persisted nowhere, and a retained selection could sit unresolved for the whole page load) and advisory a1 (the stage-1 startup log quoting the data directory per project rule 2) fixed as proposed: mode joins the protocol as a first-class gate — the sync effect returns while mode is null and re-runs the full sequence on arrival (a retained selection resolves against the real store), canSend requires mode ready, the new/send branches no-op until the adapter exists with the input staying editable, and manual pass items 19-20 verify the startup window. c3 (the usage record surviving a selection clear — the meter wore the previous conversation's reading during the fetch window and kept the deleted conversation's reading forever after a delete) fixed as proposed with both clauses: the transition's clear steps now call `chat.loadConversation(null)`, the hook's existing reset primitive that drops the record with the messages, and the meter's input is derived from the identity invariant (record shown only while `activeDoc?.id === activeId`, `?` otherwise) — the claim lives in the spec's selection-transition paragraph and usage-meter bullet, the mechanism in the work order, and manual pass item 21 checks the in-flight and post-delete cases. c4 (the A→B→A stale-completion check: an id-only comparison lets the oldest in-flight fetch for a reselected id apply over the newer transition, reopening the composer on a stale read) fixed as proposed: a per-transition token — every protocol run increments `transitionSeq` and records `{token, activeId}` as the latest transition (null transitions included), and a completion applies only when its captured `{token, id}` still equals the latest; the spec's identity bullet names the token and the work order carries the counter, the completion check, and manual pass item 22 (rapid A→B→A under throttling). all round-3 findings — the four majors and advisory a1 — are now triaged and fixed in both artifacts.
- mercurius round 4 (follow-on session s_9xfPuNnh6aHt; the MCP registry lost s_lt2MOh4s7E1Z a fourth time between rounds, the rounds themselves running through the CLI binary, and its notes having been written to disk when the registry first dropped): needs_changes — three majors and two advisories, and the review has moved from states/transitions to *timing*: the store and API remain clean in every round. PS-001 (the commit effect enqueued a full-document save on every commit and the FIFO executed all of them — a long chunked turn piled up a queue of progressively larger PUTs outliving the visible turn, so a reload mid-drain restored an older prefix with no notice, silently breaking the arc's headline promise) fixed as proposed: per-conversation save coalescing — at most one in-flight plus one pending save per id, pending replaces, the entry writes the latest pending document or no-ops — a bounded handful of PUTs per turn, and the FIFO already provides the flush semantics (transitions, deletes, clears queue behind the latest save); the spec's useSessions bullet, the hook bullet, a verification bullet, and manual pass item 23. PS-002 (the selection protocol runs in a post-render effect, so the new rail selection paints once with the old messages and an in-flight fetch can resolve between the click and the token increment), PS-003 (scenario s1 promises OS-reimage recovery, but the default data directory is erased with the OS — the arc defers carrying it), and advisories PS-A01 (a successful get can clear a failed-*list* notice while the rail is still incomplete) and PS-A02 (the store's single error channel needs sentinels to map validation/size failures to 400/413 and filesystem failures to 500) pending triage. PS-002 (the selection protocol ran in a post-render effect, so the new rail selection painted one frame with the old conversation's messages, and an in-flight fetch could resolve between the click and the token increment) fixed as proposed: the protocol's start moved into a synchronous, handler-side transition function — every selection change increments the token, aborts, and clears document/snapshot/chat state in the same batched update as the new activeId, so React renders once, already cleared, and the effect is demoted to finishing the already-recorded transition, never initiating one. PS-003 (scenario s1 over-promises OS-reimage recovery) and the deferred data-directory-carry item is named as owning the preservation. PS-A01 and PS-A02 advisories remain pending. the work order's transitions verification bullet had dropped out again (the same stale-buffer content loss as after round 2) and was re-applied with the c1 wording. the mcp server lost its session registry a third time after this round's collect; the round's notes were written directly to .mercurius/s_lt2MOh4s7E1Z/round-01/_notes.md to keep the audit trail whole.
- mercurius round 5 (follow-on session s_wP61gESCMPNU, a fresh bootstrap after yet another mcp registry loss — the rounds run through the cli binary and the on-disk round logs are the audit trail; per michael's call the agent stopped writing per-round note files): needs_changes — four majors and two advisories, and the review is now auditing the deployment boundary (tab/process teardown) that a live-tab FIFO cannot cover. michael set the round's calibration: the findings are drifting into over-hardening territory — accept the residuals, don't over-build defenses against corner cases. C1 (the unload-save window: a reload or tab close inside the coalesced-save window loses the tail, and the docs' claim that persistence never lags display was an over-promise) settled as an accepted residual per michael — no beforeunload guard built; the spec corrected the claim (display-to-disk lag bounded by one coalesced save while the tab is alive) and records a new accepted residual, unload and hard termination losing a turn's tail. C2 (a failed /api/config leaves mode null forever, silently) fixed with a sentence: no retry path, no dedicated notice — the condition is visible through the existing backend-down surfaces (empty models, failing chat, empty tools) because pane's own binary serves the request, and recovery is a restart and reload. C3 fixed as proposed (the store's `Clear` now sweeps every regular file in its private directory — the one-word change, since the directory is the store's own and an interrupted atomic write can leave a non-`*.json` temp file holding a full conversation — with a stray-file test clause, and the spec's corruption residual records the inert-orphan case: a temp file left by a crash is invisible to `List` and swept by the next clear-all). C4 fixed as proposed (`NewStore` gains a write probe — after `MkdirAll`, create, close, and remove a temporary file in the sessions directory, failing startup if any step fails — because `MkdirAll` alone succeeds on an existing directory, so the settled fail-fast decision is only real if the process can actually write where it says it will; the probe is a mechanism fix that keeps a settled promise, not new hardening). advisories A1 (clear-all's key list omitted the legacy pane:model / pane:systemPrompt keys) and A2 (how coalesced save promises settle) taken and fixed: A1 simplifies clear-all to a full `localStorage.clear()` (matching the shipped code) so the legacy keys are wiped too, in both the spec and the work order; A2 spells out that every coalesced caller settles only after the shared physical write and `savedSnapshot` advances from the document that write persisted. round 5 is fully triaged; round 6 (session s_gka3ou5xNAF8) runs next against the revised artifacts.
- mercurius round 6 (follow-on session s_gka3ou5xNAF8; its snapshot predates the round-5 A1/A2 fixes, so those were not re-raised): needs_changes — four majors, two advisories. michael directed triage one finding at a time. PS-001 (the store's `id` validation was specified as raw `encoding/json` unmarshaling into `{ID string}`, whose case-insensitive field matching accepts a differently-cased `ID` body — storing a phantom session that lists in the rail but can never open, since the frontend reads the lowercase `id` property) fixed as michael approved, taking the `dd` variant over the reviewer's raw-map suggestion: `Save` now validates through the project's `dd` JSON binding in strict mode — strict intake rejects a non-object top level, duplicate keys, trailing data, and type coercion; the document binds `ID` under an explicit `dd:"id"` tag plus a `+extra` map as the declared home for everything the store does not interpret; the bound `ID` must equal the path's id, the exact camelCase key, case-sensitive by construction. `List`'s projection decode moves to a forgiving `dd` bind with explicit camelCase name tags, exact-key in both directions. this brings the store into line with the project's JSON convention (the api layer already runs on `dd`) and enforces the arc's settled contract — the document's camelCase `id` is canonical end to end — without touching the parked camelcase-snakecase standardization card. spec: opaque-document and put-validation bullets; work order: list/save bullets, typed-error clause, store-test line, and a stage-1 verification bullet (a differently-cased `ID` PUT → 400). PS-002 (save coalescing could fold a later same-id save into an earlier queue entry that now sits ahead of a read/delete/clear enqueued in between — a write executing before an operation the tab invoked after it, the exact class of bug the round-2 FIFO was built to eliminate, reintroduced by the round-4 coalescing rule) fixed as michael approved: coalescing now applies only at the tail of the chain — a pending save absorbs a newer same-id save only while it is the last queued operation; any other operation enqueued behind it (a transition fetch, a delete, a clear, another conversation's save) forms a barrier, and the next same-id save starts a fresh entry behind the barrier in true invocation order. no new mechanism — the existing queue and the per-id invariant now compose instead of contradicting each other — and the uninterleaved long-turn case (the dominant one) still coalesces to a single physical write. the spec's useSessions bullet, the work order's hook bullet, and the stage-2 verification bullet carry the barrier clause; the verification bullet and manual pass item 23's "bounded PUT count" wording is the next finding's subject. PS-003 (the artifacts promised a bounded total PUT count per turn — "bounded handful of PUTs", item 23's "bounded number of PUTs … rather than one per commit" — which coalescing never guarantees: it bounds the *backlog* (one in-flight plus one pending per id), while the total may scale with the commit count, so a perfectly healthy loopback backend doing one prompt PUT per commit would have failed the manual pass as written) fixed as michael approved by narrowing the claim, not by building a limiter: every affected site — the spec's useSessions bullet, the work order's hook bullet, the stage-2 verification bullet, and manual pass item 23 — now asserts the backlog invariant (no pile of queued PUTs; at most one in flight and one pending at any moment) and states the total as unbounded-and-fine for a loopback appliance where a promptly completing write lands as it happened. no debounce, rate limit, or turn-boundary flush policy is added — the in-flight tail at teardown remains the round-5 accepted residual. PS-004 (the destructive-op protocols detach the commit owner before awaiting the store operation, and only a success path re-establishes anything — a 500 from a failed delete/clear left the owner detached forever: the conversation kept rendering but silently stopped persisting until the next transition or reload) settled as an accepted residual per michael — "we're already in the over-hardening territory": on a loopback appliance an operational failure of a destructive op means every save is failing too, the storage-error line is already up, and a reload re-establishes ownership, so no restore branch is built. the spec gains the residual bullet (a failed destructive operation leaves commit ownership detached) and the work order's clear-all bullet gains the clarifying clause so the success-path protocol is not misread as promising recovery. advisory PS-A01 taken as proposed: `Save` now registers `defer os.Remove(tempPath)` immediately after the successful `os.CreateTemp`, so every failure path between create and rename (write, close, chmod, rename) removes its own fragment — after a successful rename the defer is a no-op on a name that no longer exists, and a live store whose disk fills or directory goes read-only stops accumulating one debris file per failed save. the residual narrows accordingly: an orphaned temp file is now the crash case only (a dead process's fragment, which no in-process cleanup can remove), and the `Clear` sweep of the store's private directory remains as the belt-and-braces for that case. advisory PS-A02 taken as proposed: manual pass item 22's precondition ("all three fetches are in flight") described a state the global FIFO cannot produce — one adapter operation at a time, the three transition fetches queue one behind another — and was reworded to stall the first GET, let the B and A fetches queue behind it, and release; the test's assertions are unchanged, and the rewording names what the pass actually exercises: the transition token, not network overlap — the hazard being an id-only completion check letting the oldest A's document (same id as the final selection, stale token) through. round 6 is now fully triaged: four majors — PS-001 fixed (the `dd` variant, approved by michael after his dd question), PS-002 fixed (tail-only coalescing with a barrier clause), PS-003 fixed by narrowing the claim from a bounded total to a bounded backlog, PS-004 accepted as a residual (a failed destructive op leaves commit ownership detached until the next transition or reload) — and two advisories taken (PS-A01 `defer os.Remove` after `CreateTemp`, narrowing the orphan residual to the crash case; PS-A02 item 22's precondition reworded to the serialized-FIFO shape). round 7 runs next against the revised artifacts.