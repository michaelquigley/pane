# 2026-09-16

- explicit model connections built and deployed: an optional `models` registry uses its keys as strict pane-visible aliases and binds each alias to one endpoint, upstream model id, credential, context window, and output cap. endpoint and key can inherit the top-level connection, while an explicit empty profile key disables auth; profile token settings intentionally do not inherit the legacy maps or defaults. `/api/models` is configuration-authoritative in registry mode, and the legacy single-endpoint discovery path remains unchanged when the registry is absent.
- the same upstream model on multiple hosts needs two identities inside pane. usage therefore records the selected alias rather than the upstream id, which lets a mid-conversation model switch select the new host's context window on the next usage event. the production aliases are `qwen3.8-27b@eleven` at 262144 tokens and `qwen3.8-27b@fortyfive` at 163840 tokens, both targeting upstream `qwen3.8-27b` with a 24756-token output cap.
- production closeout: both hosts advertised the expected model and context metadata; the global config was migrated to the two-alias registry with its shared key preserved and mode tightened to 0600. the verified binary was atomically installed at `/home/michael/local/bin/pane`, `pane.service` restarted cleanly, and its live `/api/models` and `/api/config` responses returned the two aliases and their exact windows.
