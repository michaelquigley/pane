# 2026-09-23

- replay compatibility is accepted: resolved connection/account scope rather than alias or rotating token; incompatible continuation stays stored but is excluded from outgoing requests, and portable history remains usable. adapter and continuation design remains in `docs/future/openai-subscription-models.md`; no implementation has started.
- interruption discussion exposed a coupling: exact resumption of an interrupted qwen tool loop would require reasoning beyond the accepted request-local lifetime. the draft instead proposes explicit new-turn continuation from recorded results, with unknown tool outcomes requiring reconciliation. this proposal is not yet accepted; a durable execution ledger or background job service would expand the current stateless-chat scope.
- michael subsequently accepted that explicit recovery policy: preserve tool outcomes incrementally, retry from scratch only when non-execution is established, and continue from recorded work as a new turn after resolving unknown outcomes. transparent execution resumption and a durable execution ledger are out of scope for this integration. next decision is provider availability in the selector.
- availability policy accepted: signed-out aliases remain visible, selected-model sending is gated with cli-login guidance, unrelated models stay usable, and login is picked up without restart. registry listing stays local; credential presence does not claim entitlement or health. next discussion is provider-first configuration resolution and preventing subscription credentials from following legacy endpoint/key inheritance.
- credential isolation accepted: select provider before inheritance; subscription entries reject explicit endpoint/api-key settings and never inherit legacy destinations or keys. unsupported explicit caps must not silently disappear. behavioral decisions are consolidated in the spec; the next proposed phase is a bounded isolated verification spike, then concrete schemas/work order and mercurius review. no real login, new generation, spike implementation, or review has been authorized by this design acceptance.
- the bounded spike plan is accepted; handoff is `docs/future/openai-subscription-models-spike-brief.md`. it separates source/offline harness work from an explicit live-run approval with request/time ceilings. no spike agent has been launched and no prototype or live tests have been run here; the production work order still waits on the evidence.
- spike stage 1 done, stage 2 waiting on michael's approval; zero upstream requests made. prototype is `spike/openai-subscription/`, a separate go module on purpose so pane's `./...` gates never compile it -- don't fold it into the root module. results and the run plan are in `docs/future/openai-subscription-models-spike-results.md`.
- installed pi is now '0.87.1' (updated today), not the '0.85.1' the design cites; the results doc pins file hashes. pi behaviors deliberately not ported: cross-model thinking converted to assistant text, invented "No result provided" results for orphaned calls, websocket `previous_response_id` sessions, and defaulting sol to effort 'none'. a future agent copying pi more faithfully would break accepted decisions.
- open before any sign-in: the `originator` value (harness defaults to 'pane'; proposal is to stop rather than impersonate 'pi'/'codex_cli_rs' if rejected), how the qwen key reaches the harness (owner-only `<state>/qwen.key`), and endpoint-owner coordination -- this host is fortyfive, whose single llama.cpp slot the qwen probes would queue behind real work.
- latent production hazard found, not fixed (out of spike scope): `RunToolLoop` ignores `finish_reason`, so a `length`-truncated round containing one parseable call would still dispatch it. belongs in the work order.
- live spike run done later the same day (21 of 23 approved requests, no retries): originator 'pane' accepted; the subscription route rejects `max_output_tokens` with HTTP 400, so there is no output cap to map -- subscription `max_tokens` must stay a config error. the route also accepts empty `instructions` and forced-final rounds with function-call history but no tools. both qwen engines finish tool rounds with 'tool_calls', and active-round `reasoning_content` replay re-renders the generated prefix (next-round prompt ~96% cached).
- still unknown: live replay of encrypted reasoning. at effort 'low' on one-line prompts sol/astra emitted no reasoning items, so S4's "durable continuation" pass covers function-call items only. don't read it as proof the reasoning envelope round-trips; a small effort-'medium' follow-up is proposed in the results doc.
- private leftovers in `~/.local/state/pane-spike/` await michael's direction: a live subscription login, a copy of the qwen key, raw captures containing an account-derived `safety_identifier`. the committed live-shape fixture is sanitized.
- design review of the spike report keeps encrypted-reasoning replay as the load-bearing gap; proposed follow-up is at most three generation rounds per subscription model, six total, with actual envelope selection/transmission checked so portable fallback cannot masquerade as a pass. no follow-up or cleanup authorized here. `max_output_tokens` rejection is verified, not every possible cap mechanism; cache-hit ratios support qwen replay but do not alone prove exact prompt identity. production recovery must not inherit the prototype's synced-recorder guarantees by assumption.
- encrypted-reasoning follow-up (design-agent authorized, own ledger `budget-followup.json`, 6 of 6 requests): **pass on sol and astra** at effort 'medium'. reasoning was stored byte-equal to the stream, judged compatible after a fresh-process reload, sent byte-equal next to its companion `message` item, and accepted. caveat for the work order: both models reasoned only in the post-tool answer round, so replaying a reasoning item that precedes a `function_call` in the same round (the rs/fc pairing pi warns about) is still untested.
- design handoff: encrypted continuation is independent of visible thinking; astra's successful replay had an empty summary, so persistence cannot be gated on thinking display events. same-round reasoning/tool-call ordering has a synthetic prototype test but not a live pass. recommendation is to proceed to work-order development with that explicit verification requirement, not continue unbounded prompt hunting. private cleanup still awaits michael; no production implementation or review arc has started.
- live spike closed; no further login/inference/refresh is authorized. cleanup plain-unlinked the scratch login (not revoked upstream; the session lapses or can be revoked from the account settings), the copied qwen key, all raw captures/request bodies/per-run summaries, and the subscription conversations. it is not recoverable by ordinary means. what's left in `~/.local/state/pane-spike/` is non-sensitive: both ledgers, the fixture execution log, a sanitized `run-summaries.json`, and qwen-only synthetic docs. the repo was scanned for every credential, account, encrypted, and safety-identifier value before deletion: zero matches.
- carry into the work order: encrypted continuation must be kept and replayed regardless of its display summary (astra's replayed item had an empty summary). reasoning-before-tool-call in the same round remains live-unverified.
- first work-order draft is `docs/future/openai-subscription-models-work-order.md`, conditional on a new save-boundary decision: await a browser-owned in-progress marker save before POST, then treat unfinished hydration conservatively. today's mirror swallows queue errors and `App` only watches messages/usage, so neither awaiting the old queue nor adding a recovery field alone provides that behavior. no backend ledger proposed; no production implementation or mercurius run started.
- michael accepted the pre-send save barrier. the work order now specifies lifecycle events, recovery records, bounds, and status refresh for review; the save marker remains a conservative browser/store guarantee, not server acknowledgement of tool effects or cross-browser ownership. production implementation and new live tests remain unstarted.
