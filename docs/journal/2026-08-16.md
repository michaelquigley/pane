# 2026-08-16

- stage 2 committed by michael (afbc074). stage 3 landed (integration, verification, release notes) and gated clean on round 1 (ad-hoc terminus over truthful-naming + no-emoji — the stage diff is docs/changelog only).
- stage 3 arc, recorded per the work order:
  - stage 1 (e9bea53): backend parse/protocol/pass-through. terminus rounds 76be9dd0e017 (clean + 1 lowercase-output advisory, fixed in-round) and 201b101abe74 (clean, zero findings).
  - stage 2 (afbc074): frontend state/rendering/persistence. terminus round d0032441ce1c (clean, zero findings).
  - stage 3: docs, changelog, live verification. terminus round 9c95eb4e0e50 (clean, zero findings).
- live verification against ninfer (qwen3.8-27b on 127.0.0.1:8080, via a temporary pane instance on :8401 with a filesystem MCP server pointed at a /tmp repo; both instances torn down after the pass):
  - wire spelling confirmed: a raw /v1/chat/completions capture shows all 10 thinking chunks under `reasoning_content`, zero under `reasoning`. the vllm/sglang-family spelling is what ninfer puts on the wire; the dual tolerance holds and the capture confirms rather than decides.
  - a plain turn streamed 14 thinking_delta events live, then content deltas, round_complete, done. the round_complete assistant payload carries no reasoning field.
  - a tool-loop turn (read_file against the /tmp repo) showed one thinking block per reasoning round, each above the call it motivated: 15 thinking deltas before the round-1 tool_call_start, 5 after the tool result in round 2 before the final content. both round_complete payloads clean.
  - export acceptance: the real exportMarkdown.ts run under node over a thinking-conversation fixture — the markdown contains no thinking text, content intact, tool-only assistant messages skipped.
  - quiet model: not exercised live — this machine has no room for an additional model (michael's call after the CPU-pinned ollama attempt was abandoned). covered instead by the test suite: every tool-loop test asserts an exact event sequence with no thinking_delta, which only holds if a reasoning-free response emits nothing new, plus the render guard (the block renders only when thinking is non-empty). michael will test pane against a quiet model manually once the arc is fully landed.
- browser-only items left for michael's manual pass (no headless browser in this environment): the block resting expanded at turn end; collapse + reload returning the conversation with the block still collapsed; collapsing a committed block while a tool-loop turn is in flight and the round's committed message still landing; auto-scroll carrying a thinking-only round. the protocol layer behind each was verified over SSE in the live pass.
- docs pass: docs/current/pane.md carries the frontend half now — Message/SSEEvent types with thinking fields, the state-flow lines, the display-only paragraph (strip at request build, type-level backend guarantee, per-message collapse persistence) with the accepted residual (uncapped localStorage growth from thinking text), and the UI bullets. AGENTS.md's SSE protocol line gains thinking_delta; CLAUDE.md is a symlink to it, so one edit keeps the pair in sync. CHANGELOG.md gained the Unreleased FEATURE entry in the house prose format.
- open questions for michael: (a) the canon has no projects/pane entry — all three stages ran ad-hoc terminus over the sibling projects' quality refs; a rubric.yaml for pane would let the default command work. (b) docs/future/thinking{,-work-order}.md are now built behavior; the work order never asked to relocate them, so they stayed put.
- manual-pass polish from michael (uncommitted, in working tree):
  - thinking blocks in compacted (pulled-up tool) rounds had no bottom separator: my stage-2 rule `.message-compact .thinking-block { margin-bottom: 0 }` zeroed it while the rest of the compact layout kept spacing — exactly the rounds qwen3 thinks before. removed; every layout now uses the block's own bottom margin as the separator, and `.thinking-block + .tool-calls` takes no extra top margin.
  - label read "THINKING" (uppercase transform) — now literal lowercase `thinking...`; dropped the uppercase + letter-spacing styling.
  - per michael: both separators doubled to 24px (`margin: 24px 0`). the compact container pull-up (`-46px`) is untouched — revisit only if the stacked rounds read wrong at the new spacing.
  - rebuilt via `make build` so the installed binary embeds the fixes; the running pane instance still serves the pre-fix dist until restarted.
- stage 3 reviewed (implementation agent): content verified against the work order (AGENTS.md protocol line, CHANGELOG Unreleased FEATURE entry in house format, docs/current/ frontend synthesis with the accepted residual, installed binary 2026-08-16 02:01:37 postdates the polish edits and the rebuilt dist). suite green, vet clean. full-rubric gate on the staged tree: terminus review 353f656ebecd — clean, 0 blocking, 0 advisory. this supersedes the stage-3 ad-hoc gate above; the projects/pane rubric has existed since the 2026-08-15 session, so open question (a) above is stale — it is resolved, and stage 2 plus this re-gate ran under the rubric.
- canon review against landed code (per the implementation role, before retiring the intent docs): all six projects/pane qualities hold as written — stateless-backend (no server-side state added this arc), hand-rolled-llm-client (go.mod unchanged), thinking-display-only (verified end to end: tests, live ninfer pass, stage-2 tree re-gate; the quality's discrimination clauses match the landed wireMessages strip and unchanged round_complete exactly), tool-routing-via-registry (manager untouched this arc), zrok-embed-pattern (dist rebuilt and embedded, gitignored), dd-tag-minimalism (config untouched). no drift to flag, no unguarded invariants, no expired exemptions.
- arc-end handoff pending michael's call: the spec and work order are fully realized (docs/current/ carries the behavior and the accepted residual). per the pipeline, the two docs/future intent documents should be removed after re-synthesizing the spec's deferred items (round-trip echo, thinking control, other wire formats, export, token accounting) into a smaller docs/future note. no roadmap card points into them.
- arc-end closeout (per the pipeline, michael's go-ahead): the thinking display arc is fully realized, so the intent documents were re-synthesized out and removed. the spec's five deferred items (round-trip echo, thinking control, other wire formats, export, token accounting) plus their seam-census revisit conditions now live in docs/future/thinking-deferred.md - a menu, not a spec; it points at docs/current/pane.md for built behavior and the accepted residual. docs/future/thinking.md and thinking-work-order.md are deleted; the landing protocol and invariants list in the work order are arc machinery - the invariants are carried by the terminus-canon projects/pane rubric, the protocol by the stage-gate practice. git history preserves the removed documents. nothing pointed into them (no roadmap card; state-of-the-work does not track pane).
- calibration upkeep: mercurius.yaml review_focus reset to a self-documenting placeholder - an arc-scoped focus left pointing at retired artifacts would spend the next arc's first round reviewing the wrong target. the three settled_decisions guards (display-only-reasoning, no-thinking-control, wire-spelling-tolerance) are live project decisions, not arc artifacts, and were kept.
- roadmap adoption (michael's direction, per the grimoire roadmap convention): docs/future/roadmap/ created and the stock paragraph copied into AGENTS.md (CLAUDE.md is a symlink, so both entry points carry it), plus the package-structure line. thinking-deferred.md was removed and its five items re-landed as individual roadmap cards: round-trip-reasoning-echo-in-re-sent-history, per-model-thinking-control, other-reasoning-wire-formats (enhancement+spike), reasoning-in-the-markdown-export, reasoning-token-accounting.
- triage judgment, recorded for michael: the cards landed in state `horizon`, not `inbox` — they are settled, deliberately-resting decisions from the design session's seam census, not untriaged captures. flip them to inbox if the read should be "agent proposal, untriaged." each card carries a `## why` pointing at the journal entry that holds the arc's full context, and `subsystems` (backend/frontend) where the arc's work order made the impact clear.
- note: mercurius.yaml's review_focus reset text referred to the arc's "spec + work order in docs/future", which this commit deletes. corrected in place: it now says the intent docs were removed and synthesized into docs/current/pane.md, and the arc's deferred items live as cards in docs/future/roadmap/ - so the next arc's first round does not point at files that no longer exist.
- resolution on the "terminus/commit protocol as a project rule" candidate (raised 2026-08-15): not added to AGENTS.md. the grimoire bootstrap (agents/agent-roles, implementation section) already carries the protocol — stage gates, terminus to clean, vetoes recorded in journal/conversation — and agents here bootstrap from the grimoire. the 3-round cap and advisory handling are michael's standing per-session instruction; agent-never-commit is the practice's standing posture. a repo rule would restate the bootstrap and drift from it. the state-of-the-work update (pane's absence) is scheduled for the next planning cycle.
