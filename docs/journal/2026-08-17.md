# 2026-08-17

- context usage tracking stage 3 landed: `docs/current/pane.md`, `AGENTS.md`, both config templates, and the Unreleased changelog entry now carry the built protocol, config, conversation record, meter, and compatibility escape hatch.
- live verification against ninfer (`qwen3.8-27b` on the separate `eleven:11400` host), through a temporary pane instance on `:8401` with a filesystem MCP server pointed at a `/tmp` repo; pane, MCP child, compatibility stub, and temp repo all torn down afterward.
  - raw ninfer final chunk, verbatim: `data: {"choices":[],"created":1786982279,"id":"chatcmpl-0a57519fa2c271ef","model":"qwen3.8-27b","object":"chat.completion.chunk","usage":{"completion_tokens":41,"prompt_tokens":59,"total_tokens":100}}`
  - ninfer reports only the three scalar fields; no `completion_tokens_details` or reasoning-token breakdown. the reasoning-token-accounting card's first lookup is answered by this capture.
  - plain pane turn: one `usage` event before `round_complete`, `prompt_tokens: 2275`; meter derivation against the configured 262,144-token window is 1%.
  - filesystem tool turn over the small fixture: one usage event per round, `prompt_tokens` grew from 2306 to 2397 after the tool output entered context.
  - filesystem tool turn over the 33,878-byte built-behavior doc: one usage event per round, `prompt_tokens` grew from 2305 to 10816; the meter derivation visibly moves from 1% to 4% at ninfer's real window.
  - `/api/config` returned the configured model window and default, with `include_usage` absent.
  - compatibility stub: with usage on, a request carrying `stream_options` failed as `upstream_error` with the stub body `stub rejects stream_options`; with `include_usage: false`, the same turn completed with `delta`, `round_complete`, and `done`, no `usage` event, and `include_usage` still absent from `/api/config`.
- manual browser pass left for michael per the work order: real band colors; initial `?`; model-switch `?` then number; reload restoration; rendered stub-400 failure; mid-tool-loop abort holding `?` without a late-event flicker.
- stage 3 verification complete: `make test` green, vet clean, `make build` rebuilt and embedded the frontend before installing the developer binary. npm 11 stripped the `libc` metadata the npm 10-era lockfile carried for the optional native bindings (rolldown, lightningcss) during install.
- stage 3 terminus review `32b9f9574241`: clean, zero blocking, zero advisory.
- diagnosed the repeated post-tool-turn `HTTP 400` as pane-side request binding, not ninfer context exhaustion: assistant tool-call history legitimately carries `content: null`, while `dd v0.3.12` tried to bind that explicit null into `llm.Message.Content` as a string and rejected the request before it reached ninfer.
- provenance of the 400, recorded for the audit trail: the regression was introduced by stage 1's extension of the dd conversion to `handleChat`'s request decode (the scope extension beyond the work order), and it was accepted in the planning agent's stage 1 review — that review verified dd wire-compatibility for acronym fields, unknown request fields, and pretty-printed output, but never bound an explicit `content: null`, the one message shape the real frontend always sends on tool-call turns. stage 3's review rounds caught what the stage 1 sign-off missed; the gap was shape coverage, not method.
- implemented the field-level `+nullable` contract in `df`, including strict, forgiving, merge, nested, opaque, embedded-pointer, and `+extra` coverage. the first df Terminus pass found an embedded-field consumed-key edge case; after correction, review `c1a51b9ac241` was clean. michael committed and released the result as `df v1.0.3`.
- pane now pins `df v1.0.3` and marks only `llm.Message.Content` nullable. the handler regression sends the exact user → assistant tool call with null content → tool result → user continuation history, verifies it is not rejected, and verifies the history reaches the upstream unchanged.
- post-fix verification: the focused handler regression passed against the published module, `make test` passed with vet clean, and `make build` rebuilt the frontend before installing the Go binary. npm repeated the lockfile-only rewrite on this install, confirming the stripped form is npm 11's fixed point — a restored file is off the fixed point and gets re-stripped on the next install.
- combined stage 3 and null-content fix Terminus review `e7bae4743659`: clean, zero blocking, zero advisory.
- arc-end closeout (planning agent, per the pipeline, michael's go-ahead): the context usage tracking arc is fully realized, so the intent documents were re-synthesized out and removed. the spec's deferred items now live as roadmap cards: auto-compaction (the backend→frontend history-replacement contract; the usage event and config windows are the two facts its gate wants, and the record's placement on the conversation object means a compaction result can ride with it), local token estimation (the zero-usage fallback; it ships with the gate, so the two are separate cards that name each other), and multi-model conversation records (the per-model record map; last-wins is the shipped shape). the reasoning-token-accounting card was updated in place: its first step — the endpoint usage-shape lookup — is answered by the arc's live capture (ninfer reports only the three scalars, no completion_tokens_details), and the card now points at that capture; state stays horizon. the substrate seam's revisit condition (the usage record moves with the conversation when storage leaves the browser) was carried into the persistent-not-localstorage-sessions card, so the condition lives with the iteration it belongs to. docs/future/context-usage-tracking.md and context-usage-tracking-work-order.md are deleted; the invariants are carried by docs/current/pane.md and this file's stage-gate record, and git history preserves the removed documents.
- the three settled decisions earned in the design session, recorded for the next arc's mercurius calibration (guards to carry if the file is bootstrapped again): usage-event-three-scalars (the usage event carries exactly the three upstream scalars; the *_tokens_details sub-objects arrive with the reasoning-token-accounting card, not ahead of it — closes the event payload shape only), include-usage-backend-side (the flag shapes the upstream request, which the backend owns end to end; it never crosses /api/config — closes the config surface only), send-invalidates-usage-record (a send is a history mutation, so the meter drops to '?' before the new turn's first usage event — closes the invalidation rule only; bands, window resolution, and record placement stay reviewable). note: mercurius.yaml and .mercurius/ are absent from the tree at closeout, so the calibration reset a closeout normally performs could not be applied. if that was intentional cleanup, nothing more is needed; if not, both are gitignored operator-managed files and the next arc should bootstrap fresh.
- canon review pending (flagged for michael, per the implementation role): the arc's wire-purity invariant (the usage event carries exactly the three upstream scalars; no percentage, window, or derivation anywhere in the backend) and its record-placement invariant (the record rides the conversation object and resets on any history mutation) have no clauses in the terminus-canon projects/pane rubric — the rubric still holds the thinking arc's six qualities, last touched by the "pane terminus canon; qwen3.8 27b" commit. when the rubric is updated, the new clauses should discriminate by what they forbid, the way thinking-display-only does.
- lockfile decision (michael): commit the npm 11 form and absorb the one-time normalization. both remaining lockfile writers in the fleet emit it — the dev host (npm 11.10.0, `npm install`) and CI (node 24, `npm ci`, which never rewrites the lockfile) — so the diff settles instead of ping-ponging on every build. the lost gnu/musl discriminator on the native bindings only matters on musl hosts, of which none build pane.
- metawoo toolbar arc, combined design + planning agent (this session): the `add-the-metawoo-toolbar` card was shaped into `docs/future/metawoo-toolbar.md` (spec) and `docs/future/metawoo-toolbar-work-order.md` (work order, two stages: the bar; docs + handoff). bootstrapped from the grimoire (AGENTS.md, agent-roles, design-build-pipeline, seam-census) with the family ground truth in the archived reef and flo UIs; the grimoire's flo design-language note predates the current Source Serif 4 palette and the code is the reference of record. the seam census records four calls, all recommended for michael's confirmation: the toolbar is presentation-only (the popover's open/closed is the sole new local state); family grammar painted with pane's tokens (no reskin, no family token names); the system prompt's overflow lives in a popover, not a flo-style subbar (pane has no routes); dark mode kept. the spec's deferred menu — connection indicator, badge warm-tint, stream/approval signals, screen-reader announcer, pane-as-metawoo-product branding, settings surface — is opt-in. card gained `subsystems: [frontend]` and a spec-drawn log stamp; state stays researching until the census is confirmed and the arc moves to building.
- system-prompt overflow home, michael's call: a modal, not the popover the census recommended. the `description` glyph is the sole door to the system-prompt surface — the mode select and the custom text move into the modal the glyph opens (dimmed backdrop, centered card, z-index 30, focus trapped, escape/outside-click close, focus returns to the glyph). the mode changes only inside the modal, so the popover's auto-open-on-custom goes away; the glyph's lit state on non-default modes is the resting signal, named in its tooltip. modal markup stays inside `SystemPromptEditor` — no shared modal component until a second surface earns it. spec (overflow-home line, table, accessibility) and work order (items 3/5, critical files, invariants, handoff checklist) updated in place.
- mercurius calibration configured for the metawoo toolbar arc: the fresh bootstrap had dropped the guards the two prior arcs' closeouts designated to carry (the thinking trio display-only-reasoning / no-thinking-control / wire-spelling-tolerance, and the usage trio usage-event-three-scalars / include-usage-backend-side / send-invalidates-usage-record), so the ledger was restored from the closeout records and extended with this arc's five guards (system-prompt-modal — the operator's modal call; dark-mode-kept; pane-paint-not-reskin; deferred-menu-is-opt-in; hand-rolled-glyphs), each scoped to the argument it closes. review_focus now names the arc (spec + work order for add-the-metawoo-toolbar), states the family's baseline facts inline because the archived reef/flo UIs are outside the repo and invisible to the reviewer, and points at docs/current/pane.md plus ui/src/index.css for pane's built chrome. mercurius re-reads the file at every round, so no session reopen is needed if the calibration is amended while michael reads.
- mercurius review of the metawoo toolbar spec + work order (session s_JFBxHcjctHYY, codex gpt-5.6-sol): round 1 returned needs_changes — four major concerns, three advisories, all triaged one at a time with michael and fixed as proposed. C1: one selectedModel prop was doing two jobs (the select needs the stored override with empty = default; the meter needs the effective id) — the toolbar now takes modelOverride and selectedModel as separate props, split exactly as the header had it. C2: the tool panel's fixed top against the --toolbar-height token would be obscured once the cluster wrapped the bar taller — the panel now lives in the position:relative app-body row as absolute top/right/bottom 0, tracking the bar's rendered height; the spec says the bar promises a floor, not a fixed height. C3: invariant 4 banned restyling components the work order itself restyled — narrowed to forbid unrelated reskins and whitelist the arc's components, with the spec's substrate-scope line aligned to match. C4: a fixed tools aria-label would have taken the count away from assistive tech — the glyph's label is now dynamic (tools (n)) with an aria-hidden badge. advisories A1-A3 fixed as well: .model-selector restyled in place at the toolbar scale rather than deleted, a --shadow token added for the modal's first-use elevation, and the modal carries role=dialog, aria-modal, an accessible name, and a viewport-bounded scrolling body. round 2 over the revised artifacts: ready_to_build, zero findings, zero advisories. session closed; the audit trail is in .mercurius/s_JFBxHcjctHYY/ (round logs, snapshots, round-02 synopsis). handoff: the spec + work order are build-ready for an implementation agent; the card stays researching until implementation starts (michael flips it to building at handoff).
- self-audit of the converged spec + work order (planning agent, pre-handoff, per the pipeline): read both artifacts end-to-end and cross-checked tables against prose. eight late-sections-behind-the-final-rounds caught and fixed in place — work order: the critical-files index.css line still listed .model-selector as deleted and omitted the --shadow token (A1/A2); the Toolbar.tsx line said "selects" plural; item 2's C4 sentence had lost its terminal period; item 5's compact-select bullet still said "the two toolbar selects" and sized the mode select that now lives in the modal (consolidated to one source of truth, the modal's select keeps the carried-over .system-prompt-mode style); item 5's modal bullet named z-30 only on the backdrop (now both, DOM-order stated); item 4's "a app-layout-level" article; the panel "slides in" (no such animation exists or is added) in two places. spec: the scenario still said "the model and prompt selects" (prompt is glyph-only now); "what goes away" sent the count to the badge only (C4: badge and dynamic label); shape bullets 2 and 3 both carried the floor phrasing (bullet 2 now owns the floor, bullet 3 owns the anchoring); the overflow-home line's "open exactly while the reader is editing" overstated (aligned to the work order's no-auto-open phrasing). everything else cross-checked clean: control table vs item-2 groups, glyph provenance vs item 1, states vs invariants 6-7, census lines vs invariants 1/4/5/8, deferred menu vs the mercurius guard, props vs the real component signatures, z-order 20/10/30 vs existing css. the fixes restate converged decisions, so the round-2 ready_to_build verdict still holds; a round 3 is available if michael wants fresh eyes on the post-audit text.
