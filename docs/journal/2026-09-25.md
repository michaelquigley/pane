# 2026-09-25

- stage 2 is uncommitted. terminus round 1 ('25cf26f8a58a') found typed auth JSON binding and locked-client file placement; michael accepted both fixes. rounds 2 ('b5f2dd1c57c5') and 3 ('c14d5721ee4e') returned clean. michael then accepted the generation-timeout and cancelled-credential-save follow-up findings; review 'db34542c5478' over both fixes was clean with no new findings. no new live login, refresh, or subscription generation has run.
- `dd.MergeYAMLFile` replaces nested model-map entries across layers. the additional fieldwise model overlay preserves explicit endpoint/key presence when a higher layer changes a model to `openai-codex`; dropping that overlay can silently bypass provider isolation.
- an existing global pane config directory may be `0755`; auth keeps its file and lock at `0600` without chmodding that shared directory. new pane directories use `0700`, and group/other-writable directories are refused.
