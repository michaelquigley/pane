# 2026-09-22

- openai subscription support is in design/planning; the accepted direction and open questions now live in `docs/future/openai-subscription-models.md`. michael favors a native go provider because pane owning the conversation's tool execution is important. cli-managed login and ordinary registry/selector participation are accepted, as are switching only between turns, carrying answers and tool history across models, preserving provider continuation data with compatibility filtering, and keeping displayed thinking display-only. no work order or review arc has started.
- proceed with the pi-style subscription approach as the design direction. michael has used pi with his codex subscription for most of a year without issues; openai's [codex for open source page](https://developers.openai.com/community/codex-for-oss) explicitly names pi among supported tool choices in that program's context. this is practical evidence, not blanket contractual clearance for pane's exact integration; written clarification is not a prerequisite imposed on this design arc. no live authentication or inference has been tested for pane.
- registry-owned reasoning effort is accepted, with qwen thinking-level controls explicitly included in the same design. the older thinking-control card's lack-of-demand rationale no longer describes the operator's request. qwen3.8-27b documents effort as well as on/off control, but deployed ninfer support remains unverified; do not conflate request controls with permission to echo readable reasoning.
- subsequent infrastructure-owner report at `/home/michael/Sandbox/qwen-reasoning-controls.md` corrects the deployment assumption: eleven is ninfer, fortyfive is llama.cpp despite the shared upstream model id. its source/log/probe findings and remaining verification gaps are captured in the design draft. no new inference probes were authorized or run. ninfer's source supports 'none' through the effort field, motivating a single-field proposal; fortyfive's wire mapping and non-thinking sampling still prevent treating both endpoints identically.
- fortyfive follow-up files actually landed under `/home/michael/Sandbox/qwen38/`. source inspection now establishes top-level effort there too; cite commit '0adcc3b' / tag 'b10502', not release 'b1'. the supplied template exposes a distinction worth preserving: closed-turn reasoning can be omitted, but active tool-round reasoning becomes an empty block if not replayed. the draft proposes a request-local replay exception; michael has not accepted it, and template fidelity alone does not prove quality impact or API-required replay. hard thinking-token budgets remain a separate proposed deferral. no live probes run.
- later, fortyfive's owner completed 16 authorized prompt-only checks, no generation. `/home/michael/Sandbox/qwen3.8-request-contract.md` contains the section-9 verification supplement, not the full earlier contract. it confirms the replay boundary and forced-final placement problem. preserve the narrow distinction: successful rendering is not a successful tool loop. the suggested trailing-user recovery alternative would move the template's last-real-query boundary; the draft instead proposes adapting the initial system instruction only on the outgoing recovery request.
- subsequent discussion accepts the narrow active-qwen-tool-loop replay exception, outgoing-only initial-system recovery adjustment, and deferral of hard reasoning budgets. michael also accepts the provider/profile/effort split, with omitted provider meaning 'openai-chat-completions'; qwen engine differences belong in built-in compatibility profiles, not separate transports or arbitrary request extras. next discussion is the adapter/round boundary and durable continuation lifecycle, not implementation yet.
- adapter/round boundary is now accepted: adapters communicate with models; pane's shared loop alone approves and executes tools through mcp, then submits results through the adapter. finalized rounds and their versioned continuation envelopes are distinct from streaming previews. replay identity and interruption lifecycle remain design questions; no implementation or generation tests have started.
